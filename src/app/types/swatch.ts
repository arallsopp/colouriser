export interface Swatch {
  name: string,
  color: string,
  brightness: number,
  sepia: number,
  hue_rotate: number,
  saturation: number,
  contrast: number,
  opacity: number,
  metallic: boolean
}

export interface Palette {
  brightness: number,
  sepia: number,
  hue_rotate: number,
  saturation: number,
  contrast: number,
  opacity: number,
  metallic: boolean
}
