export interface Swatch {
  name: string,
  color: string,
  brightness: number,
  hue_rotate: number,
  saturation: number,
  contrast: number,
  opacity: number
}

export interface Palette {
  brightness: number,
  hue_rotate: number,
  saturation: number,
  contrast: number,
  opacity: number
}
